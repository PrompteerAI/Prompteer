{
  "name": "prompteer",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.12.1",
  "scripts": {
    "prepare": "husky || true",
    "dev": "bash -lc 'set -a; [ -f .env ] && . ./.env; set +a; trap \"kill 0\" INT TERM EXIT; (cd apps/api && uv run fastapi dev app/main.py --port 8000) & pnpm --filter @prompteer/web dev & wait'",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "commitlint": "commitlint --edit"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.8.1",
    "@commitlint/config-conventional": "^19.8.1",
    "husky": "^9.1.7",
    "lint-staged": "^15.5.2",
    "prettier": "^3.5.3",
    "turbo": "^2.5.4"
  }
}
